package DSA;

//add(E e): Adds an element to the back of the queue.
//This method is similar to offer, but it throws an exception if the queue is full.

//element(): Retrieves the element at the front of the queue without removing it.
//Throws a NoSuchElementException if the queue is empty.

//remove(): Removes and retrieves the element at the front of the queue.
//Throws a NoSuchElementException if the queue is empty.

//poll(long timeout, TimeUnit unit): Retrieves and removes the element at the front of the queue,
//waiting for the specified time if the queue is empty. Returns null if the timeout is reached.

//peek(): Retrieves the element at the front of the queue without removing it.
//Returns null if the queue is empty.

//clear(): Removes all elements from the queue, leaving it empty.

//contains(Object o): Checks if the queue contains a specific element.

//iterator(): Returns an iterator for iterating over the elements in the queue.
//This method allows you to use a for-each loop to traverse the queue.

//toArray(): Converts the queue to an array of objects.
//The order of elements in the array depends on the specific implementation of the queue.

//toArray(T[] a): Converts the queue to an array of a specified type
//and fills the provided array if it has enough space.
public class QueueMethods {

}
